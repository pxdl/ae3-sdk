"""ae3tools -- desktop toolkit for Ape Escape 3 (PS2) data.

Importable library + the `ae3` CLI. Reads everything from the user's own game
disc; ships no game data. See docs/formats/ for the format specifications these
implementations follow.
"""
__version__ = "0.1.0"
